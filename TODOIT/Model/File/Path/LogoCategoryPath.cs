using System.Collections.Generic;
using FileHelper.Model;

namespace TODOIT.Model.File.Path
{/*
    public class LogoCategoryPath : LocalPathDirectoryProvider
    {
        
        public LogoCategoryPath(Category category)
        {
            _category = category;
        }

        public const string CategoryDirectory = nameof(Category);
        public const string Logo = nameof(Logo);


        private readonly Category _category;


        public override IEnumerable<string> PathElements
        {
            get
            {
                yield return CategoryDirectory;
                yield return _category.Id.ToString();
                yield return Logo;
            }
        }

   
} */
}
